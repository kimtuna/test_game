# design.md — 초기 기획서

이 문서는 거의 고치지 않는다. 게임의 목적과 범위를 바꾸고 싶으면 `inbox.md`에 근거와 함께 지시를 남기고, 여기는 그 지시가 승인된 뒤에만 최소한으로 갱신한다.

## 게임

- 제목(가칭): Test Game
- 장르: 탑다운 2D 미니 수집 게임 (프로토타입)
- 엔진: Godot 4.7, GDScript
- 플랫폼: 데스크톱(macOS), 키보드 조작

## 핵심 루프

1. 플레이어가 방향키로 맵을 돌아다닌다.
2. 맵에 흩어진 아이템을 접촉해서 모은다.
3. 아이템을 모을 때마다 점수가 오르고 화면에 표시된다.
4. 정해진 수의 아이템을 다 모으면 "클리어" 화면이 뜬다.

이 루프 하나가 끝까지 동작하는 것이 1차 목표다. 그 이후 확장(적, 타이머, 여러 레벨 등)은 `inbox.md`로 지시가 내려온 뒤에 진행한다.

## 현재 포함된 것 (하네스 시작 시점)

- `CharacterBody2D` 기반 Player, 방향키 이동 (`scripts/player.gd`)
- `Main.tscn`에 Player 배치

## 범위 밖 (지시 없이는 만들지 않음)

- 멀티플레이, 세이브/로드, 사운드/음악, 모바일 대응, 복잡한 아트

## 완료 정의 (이번 프로토타입의 "끝")

- 아이템 스폰 → 수집 → 점수 표시 → 전부 모으면 클리어 화면, 이 흐름이 `godot --headless --quit` 통과 + 실제 실행에서 처음부터 끝까지 끊김 없이 동작하면 1차 완료로 본다.
