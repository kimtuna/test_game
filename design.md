# design.md — 초기 기획서

이 문서는 거의 고치지 않는다. 게임의 목적과 범위를 바꾸고 싶으면 `inbox.md`에 근거와 함께 지시를 남기고, 여기는 그 지시가 승인된 뒤에만 최소한으로 갱신한다.

> 2026-09-01: 사용자가 게임 방향을 처음부터 다시 설명해서 이 문서를 전면 개편했다. 이전 버전(탑다운 미니 수집 게임 프로토타입)은 하네스 동작을 검증하기 위한 임시 목표였을 뿐, 아래가 실제로 만들고자 하는 게임이다. 기존에 만들어진 아이템 수집/점수/클리어 화면 코드는 이 기획과 무관하므로, 재사용 가능한 부분(캐릭터 이동 등)만 남기고 나머지는 하네스가 알아서 정리한다.

## 게임

- 제목: (미정)
- 장르: 나만의 섬 라이프시뮬레이션 (동물의 숲 스타일)
- 엔진: Godot 4.7, GDScript
- 플랫폼: 데스크톱, 키보드 조작, 세션 서버 기반 멀티플레이

## 세계관 / 핵심 컨셉

- 플레이어는 자신의 섬을 가진다. 섬에는 다양한 식물, 나무, 동물, 물고기가 서식한다.
- 퀘스트는 없다. 처음에 튜토리얼로 기본 조작과 시스템을 안내하고, 그 이후로는 유저가 스스로 하고 싶은 것을 정해서 자유롭게 플레이한다.
- 보스는 없다. 대신 식물/나무/동물/물고기 각각에 **등급**이 존재하고, 높은 등급일수록 잡거나 채집하기 어렵다. 유저는 더 높은 등급을 상대하기 위해 장비를 맞춰(강화/교체) 나간다.

## 동물 시스템

- 동물은 죽일 수도 있고, **포획**할 수도 있다.
- 포획 조건: 동물의 체력(피)이 8% 미만으로 떨어진 상태에서 마취총을 쏘면 포획된다.
- 동물 AI — 도주 트리거:
  - 사람의 발소리를 감지했을 때
  - 동물의 시야 안에 사람이 보일 때
  - 공격(피격)당했을 때
  - 위 조건 중 하나라도 해당하면 동물은 도망간다.

## 멀티플레이

- 세션 서버 방식으로 멀티플레이를 지원한다. (세부 아키텍처는 미정 — 실제 구현 단계에서 하네스가 기술적으로 판단하되, 큰 방향이 바뀌는 결정이면 `status.md`에 근거와 함께 남긴다.)

## 캐릭터

- 캐릭터 슬롯: 코어키퍼(Core Keeper)처럼 계정당 3개의 캐릭터 슬롯을 만들 수 있다.
- 커스터마이징: 캐릭터 외형을 커스터마이징할 수 있다.
- 기본 코디(의상) 제공: 별도로 맞추지 않아도 입고 시작할 수 있는 기본 복장이 있다.

## 범위 밖 / 아직 정해지지 않음 (지시 없이는 임의로 확정하지 않음)

- 구체적인 등급 체계(몇 단계인지, 수치 등), 장비 종류와 강화 방식의 세부, 튜토리얼의 정확한 내용, 마취총 재장전/사거리 등 밸런스 수치, 섬의 크기·지형 생성 방식, 아트 스타일/그래픽 리소스 — 이런 세부는 명시되지 않았으므로, 하네스는 규칙 1(합격 기준)을 지키는 선에서 상식적인 기본값으로 최소 구현하고, 중요한 방향 결정은 `status.md`에 근거와 함께 남겨 다음 세션이나 사용자가 검토할 수 있게 한다.
- 사운드/음악, 모바일 대응은 별도 지시가 없는 한 후순위.

## 진행 방식에 대한 참고

이 기획은 기존 "1차 완료 정의(스폰→수집→점수→클리어)"처럼 하나의 짧은 루프로 끝나지 않는, 훨씬 큰 범위의 게임이다. 캐릭터 이동/카메라 → 섬 기본 지형 → 채집/사냥/포획 → 등급·장비 → 튜토리얼 → 캐릭터 커스터마이징/슬롯 → 멀티플레이 순으로, 작은 단위로 나눠 여러 세션에 걸쳐 만들어 나간다. 다음에 무엇을 먼저 만들지는 `status.md`의 누적 기록과 `inbox.md`의 사용자 지시로 정한다.
