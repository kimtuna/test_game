# qa_test.md — QA 테스트 기록

`status.md`(무엇을 만들었는지)와는 별개로, **QA를 실제로 언제, 무엇을, 어떻게 테스트했고 결과가 뭐였는지**만 기록하는 파일이다. 새 항목은 번호를 이어서 맨 아래에 추가한다. 기존 항목은 수정/삭제하지 않는다. QA 중 문제를 발견하면 `inbox.md`에 지시로 등록하고, 그 inbox 번호를 여기 같이 남긴다.

---

### #1 — 2026-09-02 06:24 (사람이 직접 실행)

**범위**: 전체 QA — 헤드리스 테스트 전체 재실행 + 실제 창 실행 확인.

**헤드리스 테스트 (35개, 2-프로세스/헬퍼 스크립트 제외)**:
- 통과 33개
- 실패 2개: `animal_capture_headless_test`, `animal_hunt_headless_test`
  - 원인: 새 버그가 아니라 기존에 이미 여러 세션에 걸쳐 관찰·기록된 결함(`status.md #54/#66/#73/#75`, `status_archive.md` 730번 줄 근처) — 테스트가 `Input.action_press`/`action_release` + `await process_frame`로 클릭을 흉내내는 방식인데, 가끔 한 번의 "클릭"이 물리 프레임 두 개에 걸쳐 두 번 처리되는 비결정적 타이밍 버그. 실제 게임 로직이 아니라 **테스트 코드 자체의 결함**으로 이미 여러 차례 확인됨(`git stash`로 변경 전 코드에서도 동일 재현됨).

**실제 창 실행 확인**:
- `godot --path .`로 실제 창을 띄워 프로세스가 크래시 없이 정상 기동하는지 확인 — 정상 기동, 렌더러(OpenGL 4.1 Metal Compatibility, Apple M1) 정상 초기화, 에러 로그 없음.

> [!CAUTION]
> `screencapture`로 실제 스크린샷을 찍으려 했으나 `could not create image from display` 에러로 실패했다(macOS Screen Recording 권한이 이 세션/터미널 프로세스에 부여되지 않은 것으로 보임 — 재시도해도 동일). 하네스가 이전에 같은 문제를 겪었을 때는 Godot 엔진 내부 캡처(`get_viewport().get_texture()`)로 대체했는데, 이번엔 이미 떠 있는 프로세스에 사후적으로 캡처 명령을 주입할 방법이 마땅치 않아 스킵했다. **결과: 창이 뜨고 크래시 없이 도는 것까지는 확인했지만, 화면에 실제로 뭐가 보이는지(해상도 변경 반영, 사슴 방향/배회 등)는 이번 QA에서 시각적으로 재확인하지 못했다.** 이 부분은 사람이 직접 `test_game-play`로 열어 확인하거나, 스크린 레코딩 권한을 터미널에 부여한 뒤 다시 QA를 돌려야 한다.

**발견된 문제 / inbox 등록**: 위 CAUTION(screencapture 권한 문제)을 제외하면 새로 발견된 문제 없음. 기존에 알려진 `animal_hunt`/`animal_capture` 테스트 플레이키니스는 `inbox.md #14`로 새로 등록해 다음 사이클에서 조사하도록 했다(아래 참고).

**관련 inbox**: `#14` (테스트 플레이키니스 조사 지시)
