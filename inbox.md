# inbox.md — 지시함

사용자가 직접 남기는 지시/아이디어. 새 항목은 번호를 이어서 맨 아래에 추가한다. 하네스는 이 중 아직 "처리 완료"로 표시되지 않은 가장 오래된 항목부터 반영하고, `status.md`에 반영 내역을 남긴 뒤 여기에 처리 완료 표시를 추가한다.

형식: `### #N — YYYY-MM-DD HH:MM\n지시 내용 + 근거(왜 원하는지)\n상태: 미처리 / 처리 완료 (status.md #N)`

---

### #1 — 2026-09-01

design.md를 "동물의 숲 스타일 나만의 섬" 기획으로 전면 교체했다. 근거: 이전 design.md(탑다운 아이템 수집 프로토타입)는 하네스 동작을 시험해보기 위한 임시 목표였고, 실제로 만들고 싶은 게임은 새 design.md에 적힌 내용이다.

지시 내용:
1. 기존에 만들어진 아이템 수집/점수 표시/클리어 화면(Item.tscn, item.gd, Main.tscn의 점수·클리어 로직, tests/loop_headless_test.gd 등)은 새 기획과 관련이 없다. Player 이동 스크립트처럼 새 기획에서도 그대로 쓸 수 있는 부분은 남기고, 그렇지 않은 부분은 다음 세션에서 정리(삭제 또는 새 기획에 맞게 재작성)해도 된다. 지우기 전에 QA가 깨지지 않는지 확인할 것.
2. 새 design.md 기준으로 "진행 방식에 대한 참고" 섹션에 적힌 순서(캐릭터 이동/카메라 → 섬 기본 지형 → 채집/사냥/포획 → 등급·장비 → 튜토리얼 → 캐릭터 커스터마이징/슬롯 → 멀티플레이)를 큰 그림으로 참고하되, 매 세션 규칙 4(기능 하나만)를 지켜서 가장 작고 확실하게 만들 수 있는 한 조각부터 시작할 것.
3. 세부 수치·아트·구체적 시스템 설계는 design.md의 "범위 밖 / 아직 정해지지 않음"에 적힌 대로, 하네스가 상식적으로 판단해서 진행하고 중요한 결정은 status.md에 근거와 함께 남길 것.

상태: 처리 완료 (status.md #12) — 지시 1번(구 프로토타입 정리)을 이번 세션에서 처리함. 지시 2·3번(로드맵 순서 참고, 세부 판단 기준)은 일회성 작업이 아니라 design.md에 이미 반영된 진행 방식/범위 밖 처리 원칙이므로 앞으로도 매 세션 계속 참고한다.

---

### #2 — 2026-09-01 11:38

`status.md`/`inbox.md` 항목 제목에 날짜만 있고 시간이 없어서 기록을 봐도 언제 있었던 일인지, 세션 간 간격이 얼마인지 알기 어렵다. 근거: 사용자가 진행 상황을 확인할 때 날짜만으로는 부족하고, 실제로 한 세션이 몇 분 만에 끝나는 경우가 많아 시간 단위 구분이 필요하다.

지시 내용: 이제부터 `status.md`/`inbox.md`에 새 항목을 쓸 때 제목에 `YYYY-MM-DD HH:MM` 형식으로 시간까지 적을 것. (`CLAUDE.md`에 이미 규칙과 형식을 반영해뒀다 — `date '+%Y-%m-%d %H:%M'`로 얻은 값 사용.) 과거 항목(#1~#N)은 소급해서 고치지 않는다 (근거: 기존 항목 수정 금지 규칙과 동일 — 과거 기록은 있는 그대로 둔다).

상태: 처리 완료 (CLAUDE.md에 규칙 반영 — 사용자가 직접 지시한 문서 규칙 변경이라 다음 자동 세션을 기다리지 않고 바로 적용함)

---

### #3 — 2026-09-01 11:46

에러가 나거나 QA/확인 과정에서 문제가 생겼다가 해결된 경우, `status.md` 기록에서 그 부분을 빨간색으로 강조하고 싶다. 근거: GitHub에서 기록을 훑어볼 때 문제 없이 넘어간 세션과 에러가 있었던 세션을 한눈에 구분하고 싶다.

지시 내용: `status.md`는 GitHub `.md` 파일로 렌더링되므로, 일반 마크다운 색상 지정 대신 GitHub 전용 Alert 문법 `> [!CAUTION]`을 사용할 것 (렌더링하면 빨간 테두리 + 아이콘 박스로 표시됨). 이번 세션에서 에러/QA 실패 → 해결 흐름이 있었다면, `status.md`의 해당 항목 안에 다음 형식으로 적을 것:
```
> [!CAUTION]
> 무엇이 문제였는지 — 원인 — 어떻게 고쳤는지 — 재확인 결과
```
(`CLAUDE.md`에 이미 규칙과 형식을 반영해뒀다.) 문제가 없었던 세션에는 이 블록을 넣지 않는다 (근거: 실제로 없었던 문제를 있는 것처럼 보이게 하면 강조의 의미가 없어진다).

상태: 처리 완료 (CLAUDE.md에 규칙 반영 — 사용자가 직접 지시한 문서 규칙 변경이라 다음 자동 세션을 기다리지 않고 바로 적용함)

---

### #4 — 2026-09-02 01:55

배경: 사용자가 에디터를 열어 직접 플레이해보고 조작 체계를 지시함. 확인해보니 코드 어디에도 `mouse_mode`/마우스 캡처 설정이 없다 — "게임 시작부터 마우스 클릭이 안 된다"는 건 버그가 아니라, 애초에 마우스 입력을 처리하는 코드 자체가 아직 하나도 없어서다(모든 상호작용이 지금은 `ui_accept`(Space/Enter) 하나로만 되어 있음). 아래 2·3번을 구현하면 자연히 해결된다.

지시 내용 (우선순위 순 — 규칙 4(기능 하나만)에 따라 여러 세션에 걸쳐 순서대로 처리할 것):

1. **이동을 WASD로 변경**: 현재 `scripts/player.gd`가 `Input.get_vector("ui_left","ui_right","ui_up","ui_down")`(방향키 기반 기본 액션)를 쓰고 있다. W/A/S/D로 바꿀 것.
2. **사냥(동물 대상)을 마우스 기반으로 변경**:
   - 좌클릭 = 발사(현재 장착된 총으로 사격)
   - 우클릭 = 탄종류 변경 슬롯 UI를 띄워 탄 종류(예: 일반탄=공격/사살용, 마취탄=포획용) 선택
   - R = 재장전
   - 지금 `scripts/animal.gd`는 `ui_accept`로 공격(`_attack`, tool 장비 사용)과 포획 시도(마취총, weapon 장비 사용)를 처리하고 있다(player.gd 주석 4~9번 줄, animal.gd 52~57번 줄 참고). 이 좌클릭 발사 입력이 지금의 공격/포획 로직(등급 체크, 체력 8% 미만 포획 조건 등)을 그대로 트리거하도록 대체하면 된다 — 판정 로직 자체는 이미 있으니 입력 방식만 바꾸는 것.
3. **그 외 상호작용(채집/벌목: Tree/Plant/Fish)은 지금처럼 스페이스바(`ui_accept`) 유지**. 새로 바꾸지 말 것.
4. **메인 메뉴 + 저장/불러오기 시스템**: 게임 실행 시 바로 플레이 화면으로 들어가지 말고, 슬롯 선택 화면(코어키퍼처럼 3개 슬롯, design.md에 이미 명시된 사양)을 먼저 보여줄 것. 새 슬롯 선택 시 커스터마이징 화면부터 시작, 기존에 저장된 슬롯 선택 시 저장된 상태 그대로 이어서 시작. 설정 메뉴, 종료 메뉴도 필요.
5. **범위 제외 (이번엔 하지 말 것)**: 제작(크래프팅), 아이템 줍기, 인벤토리, 상자 정리는 design.md에도 없고 사용자가 명시적으로 나중으로 미뤘다. 지금은 기본 게임 루프(이동/전투/채집/메뉴/저장)부터 잡는다.

근거: "일단 지금은 되야 하는게 맞다"(사용자) — 지금까지 헤드리스 테스트로 로직만 검증됐지, 사람이 실제로 조작할 수 있는 입력 방식과 메뉴/저장 흐름이 없어서 실제로 플레이할 수 있는 상태가 아니었다. design.md의 "캐릭터 슬롯 3개, 커스터마이징, 기본 코디"도 메뉴/저장 흐름이 있어야 의미가 생긴다.

상태: 처리 완료 (1번 WASD 이동은 status.md #48, 2번 마우스 기반 사냥(좌클릭 발사/우클릭 탄종류/R 재장전)은 status.md #49, 4번 메인 메뉴+슬롯 선택+저장/불러오기는 status.md #50에서 반영. 3번(채집은 스페이스 유지)은 애초에 건드리지 않아 그대로 충족됨. 5번(제작/줍기/인벤토리 정리 제외)은 "하지 말 것" 지시라 미착수 상태 자체가 이행임.)

---

### #5 — 2026-09-02 02:36

배경: 사용자가 직접 플레이해보고 "총인데 직접 동물에 다가가서 때려야 되게 만들어져있다"고 지적함. 코드로 확인해보니 사실이었다 — `scripts/player.gd` 108~110번 줄 주석에 "발사(좌클릭)는 animal.gd가 `player_nearby`로 근접 여부를 이미 판정하고 있으므로 그쪽에서 직접 처리"라고 명시돼 있고, `scripts/animal.gd`는 근접 `Area2D`(`player_nearby`, 상호작용 반경 50)에 들어와 있을 때만 발사가 유효 판정된다. 즉 inbox #4의 "마우스 기반 사냥"을 구현할 때 입력 방식(좌클릭 트리거)만 바뀌었을 뿐, 판정 자체는 기존 근접(space로 때리던 시절) 로직을 그대로 재사용해서 총이라는 개념과 실제 동작이 어긋난다.

지시 내용: 총이라면 실제로 사거리가 있어야 한다. 근접(`player_nearby`) 여부가 아니라, 플레이어 위치에서 조준 방향(마우스 커서 방향 또는 현재 조준 방향)으로 일정 사거리 안에 있는 동물을 맞히도록 판정 로직을 바꿀 것. 구체적 구현 방식(레이캐스트, 거리+각도 체크 등)은 하네스가 Godot 4 기준으로 판단해서 진행. 기존에 이미 있는 게이트 로직(탄종류에 따라 공격/포획 결정, 장비 등급 체크, 체력 8% 미만 포획 조건 등)은 그대로 유지하고, "무엇을 맞힐 수 있는가"의 조건만 근접에서 사거리로 바꾸면 된다.

근거: design.md에도 "마취총을 쏘면 포획"이라고 명시돼 있고, 사용자가 명시적으로 총 컨셉을 요청했다(inbox #4) — 총인데 사거리가 없으면 이름과 실제 플레이가 맞지 않는다.

상태: 처리 완료 (status.md #53)

---

### #6 — 2026-09-02 03:00

배경: 사용자가 직접 플레이해보고 "총이 제대로 작동이 안된다"고 지적함. 코드로 원인을 확인했다 — `scripts/player.gd`의 `facing_direction`(발사 조준 방향으로 쓰이는 값)이 48번 줄 주석에 명시된 대로 "실제 마우스 커서 방향 대신 현재 이동 방향" 기준으로 계산되고 있었다. 즉 마우스로 동물을 조준해서 클릭해도, 실제 판정은 캐릭터가 마지막으로 걸었던 방향 기준이라 마우스 조준과 무관하게 맞거나 빗나간다. 이게 사용자가 "제대로 작동 안 한다"고 느낀 직접적 원인으로 보인다.

지시 내용 (우선순위 순 — 규칙 4(기능 하나만)에 따라 여러 세션에 걸쳐 순서대로 처리할 것):

1. **[최우선, 버그] 조준 방향을 실제 마우스 커서 방향으로 계산하도록 수정**: `_find_fire_target()`이 쓰는 조준 기준을 `facing_direction`(이동 방향)이 아니라, 플레이어 위치에서 마우스 커서의 월드 좌표(`get_global_mouse_position()`)를 향하는 방향으로 바꿀 것. 창 크기 조절/스트레치 설정(`window/stretch/mode="canvas_items"`)이 걸려있으니 마우스 좌표 변환이 실제로 맞는지 QA에서 확인할 것.
2. **[최우선] 총을 들고 있으면 조준선(에임 라인/크로스헤어) 표시**: 위 1번과 같은 조준 방향 데이터를 써서, 플레이어에서 마우스 커서 쪽으로 얇은 선(또는 커서 위치에 작은 조준점)을 그릴 것. 2D 게임이라 사용자가 실제로 어디를 조준하는지 눈으로 봐야 한다는 요구.
3. **인벤토리 (E키)**: E를 누르면 인벤토리 창이 열리고 닫힌다. 마인크래프트 인벤토리, 코어키퍼 인벤토리를 참고할 것(그리드형 슬롯, 아이템 아이콘+개수 표시). 기본 용량(가방 없이): **9칸**.
4. **장비 슬롯 (착용형)**: 모자 / 상의 / 하의 / 신발 / 귀걸이 / 반지 / 가방 — 이렇게 7개 슬롯으로 착용 가능하게 만들 것. (참고: `player.gd`에 이미 있는 `tool`/`weapon`/`rod`/`sickle` 장비 딕셔너리와는 별개 개념이다 — 그건 "도구", 이번 건 "복장/악세서리". 기존 `equipment` 딕셔너리를 확장하든 별도로 두든 하네스가 Godot 구조상 적절히 판단.)
5. **휴대 장비 핫바 (숫자 1~5)**: 키보드 1~5는 인벤토리와는 별도의 "들고다니는 장비" 칸이다. 인벤토리 그리드 9칸과 섞이지 않게 UI/데이터 구조를 분리할 것.

근거: design.md에 캐릭터 커스터마이징/장비는 이미 명시돼 있고, 사용자가 실제 플레이 경험을 바탕으로 구체적 스펙(슬롯 종류, 용량 9칸, 참고 게임)까지 지정해서 지시함.

상태: 처리 완료 (1번·2번은 status.md #54, 3번(인벤토리, E키, 9칸)은 status.md #55, 4번(장비 슬롯 7종)은 status.md #56, 5번(핫바, 숫자 1~5)은 status.md #57에서 반영. 5개 항목 모두 반영 완료.)

---

### #7 — 2026-09-02 03:49

배경: 사용자가 직접 플레이해보고 3가지를 요청함.

지시 내용 (우선순위 순 — 규칙 4(기능 하나만)에 따라 여러 세션에 걸쳐 순서대로 처리할 것):

1. **[제거] 게임 중 Tab 키로 캐릭터 슬롯 전환 메뉴 열리는 것 제거**: `scripts/main.gd`의 `_unhandled_input`(약 315번 줄)에 `if event.keycode == KEY_TAB: slot_overlay.visible = true`가 있다. 이 동작을 제거할 것. 슬롯 선택은 메인 메뉴(게임 시작 시)에서만 하고, 플레이 중 Tab으로 슬롯을 바꾸는 기능 자체를 없앤다.
2. **상호작용을 전부 마우스 좌클릭으로 통일**: 지금 `scripts/tree.gd`/`scripts/plant.gd`/`scripts/fish.gd`는 `Input.is_action_just_pressed("ui_accept")`(스페이스/엔터) + `player_nearby`(근접)로 채집/낚시를 처리하고 있다. `scripts/animal.gd`는 이미 좌클릭 발사(사거리+조준각) 기반으로 바뀌어 있다(inbox #5/#6). 사용자 판단: 어차피 도구/무기를 장비로 드는 구조이니, 상호작용은 전부 좌클릭(`fire` 액션)으로 통일해도 된다. 나무/식물/물고기는 근접 도구(도끼/낫/낚싯대)로 쓰는 거라 총처럼 긴 사거리일 필요는 없다 — 근접 판정(`player_nearby`)은 유지하되 트리거 키만 `ui_accept`에서 좌클릭(`fire`)으로 바꾸는 정도가 자연스러워 보인다. 다만 최종 판단은 하네스가 Godot 구조상 알맞게 하되, "스페이스바로 하는 상호작용이 하나도 남지 않게" 하는 게 목표다.
3. **캐릭터 커스터마이징 범위를 눈 / 피부색 / 머리카락종류로 확장**: 지금은 `BODY_COLOR_CHOICES`로 몸 색 하나만 고를 수 있다(`scripts/main.gd`, `scripts/player.gd`). 이걸 세 가지로 나눌 것: (a) 눈(색 또는 모양 중 도트로 표현 가능한 최소 단위), (b) 피부색, (c) 머리카락 종류(2~3종 정도의 절차적 도트 패턴이나 색). 게임이 2D 도트(픽셀아트) 스타일이라 표현이 단순할 수밖에 없다는 걸 감안해서, 상식적인 선에서 최소 구현할 것. 저장/불러오기 시스템(inbox #4 4번)에 이 세 항목이 슬롯별로 저장되도록 포함시킬 것.

근거: 사용자가 직접 플레이해보고 발견한 불편함(원치 않는 Tab 메뉴)과, 장비 시스템이 갖춰진 만큼 상호작용 방식을 일관되게 정리하고 싶다는 요청, 그리고 캐릭터 커스터마이징을 게임의 도트 그래픽 스타일에 맞게 구체화하고 싶다는 요청.

상태: 처리 완료 (1번은 status.md #59, 2번은 status.md #60, 3번은 status.md #61에서 반영. 3개 항목 모두 반영 완료.)

---

### #8 — 2026-09-02 04:09

배경: 지금까지 캐릭터/아이템은 전부 절차적으로 그린 단색 사각형이다. 실제 도트(픽셀아트) 그림을 넣고, 걷는 것처럼 움직여야 하는 대상은 자연스러운 애니메이션까지 만들어야 한다. 사용자와 상의한 결과, Python(Pillow)으로 그림을 생성하는 파이프라인을 하네스에 만들기로 했다. `python3` 실행 권한을 하네스 러너의 `--allowedTools`에 추가해뒀다(Bash(python3 *)). Pillow는 이 기기에 이미 설치되어 있다(`python3 -c "import PIL"`로 확인 가능).

지시 내용 (우선순위 순 — 규칙 4(기능 하나만)에 따라 여러 세션에 걸쳐 순서대로 처리할 것):

1. **아트 스타일 가이드를 먼저 확립하고 문서로 남길 것**: 본격적으로 스프라이트를 만들기 전에, 캔버스 크기(예: 32x32), 색상 팔레트(색 개수 제한 등), 외곽선 유무/두께 같은 도트 그림의 기본 규칙을 정해서 `assets/ART_STYLE.md`(또는 하네스가 적절하다고 판단하는 위치)에 문서로 남길 것. **이후 모든 스프라이트 생성 세션은 반드시 이 문서를 먼저 읽고 그대로 따라야 한다** — `design.md`/`CLAUDE.md`처럼 계속 참고하는 기준 문서로 취급한다. (근거: 사용자가 "전체적으로 디자인의 느낌이 비슷해야 한다"고 명시했다. 세션마다 기억이 없으니, 스타일이 문서로 고정돼 있지 않으면 스프라이트마다 그림체가 제각각이 된다.)
2. **Python 기반 스프라이트 생성 도구 만들기**: Pillow로 픽셀 단위로 그리는 재사용 가능한 생성 스크립트를 만든다(위치는 하네스가 적절히 판단, 예: `tools/`). 이 도구는 하나의 대상(캐릭터 몸통, 아이템, 동물 등)에 대해 **후보 이미지를 10장 생성**할 수 있어야 한다(예: 색상/비례/디테일 등을 조금씩 다르게 절차적으로 변주).
3. **생성 → 비교 → 선택을 표준 절차로 만들기**: 스프라이트가 필요할 때마다 (a) 후보 10장을 생성하고, (b) 한 장의 비교 이미지(그리드나 스트립으로 합친 것)로 만들어서, (c) **Read 도구로 그 이미지를 실제로 열어보고(멀티모달로 시각적으로 확인 가능하다) 눈으로 비교해서 가장 나아 보이는 것을 고른다** — 그냥 임의로 하나 고르거나 프로그램적 점수만으로 정하지 않는다. 10장이 다 별로면 파라미터를 조정해서 다시 생성한다. 왜 그걸 골랐는지 `status.md`에 근거를 남긴다.
4. **움직이는 대상은 애니메이션 자연스러움 확인 루프를 추가로 거칠 것**: 플레이어/동물처럼 여러 프레임(걷기 등)이 필요한 대상은, 프레임을 순서대로 나열한 스트립 이미지를 만들어 **Read 도구로 직접 보고** 동작이 자연스러운지(비례가 프레임마다 일관적인지, 움직임의 흐름이 부드러운지) 판단한다. 부자연스러우면 프레임을 다시 생성하거나 수정한 뒤 다시 이 확인을 반복한다 — 자연스러워 보일 때까지. 정적인 대상(아이템 등)은 이 단계가 필요 없다.
5. **첫 적용 대상은 플레이어 캐릭터**: 파이프라인(1~4번)이 준비되면, 첫 실제 적용 대상으로 플레이어 캐릭터의 정적 스프라이트(피부/눈/머리 커스터마이징이 반영된 형태)부터 위 절차로 만든다. 이후 세션에서 걷기 애니메이션, 다른 대상(동물/나무/아이템 등)으로 순서대로 확장한다.

근거: 지금까지는 색깔 있는 사각형뿐이라 실제 게임처럼 보이지 않는다. 후보를 여러 장 만들고 실제로 시각적으로 비교/판단하는 과정이 있어야 결과물이 규칙 1(합격 기준)에 부합하고, 스타일 가이드가 있어야 여러 세션에 걸쳐 만들어지는 스프라이트들이 서로 다른 그림체로 튀지 않는다.

상태: 처리 완료 (1번 아트 스타일 가이드 문서화는 status.md #62, 2번 Pillow 생성 도구(tools/sprite_gen.py)는 status.md #63, 3번 비교 그리드 도구+시각 선택(player_body_base #08 선정)은 status.md #64, 5번 플레이어 캐릭터 첫 적용(실제 도트 실루엣으로 교체)은 status.md #65, 4번 애니메이션 자연스러움 확인 루프(플레이어 걷기 4프레임)는 status.md #66에서 반영. 5개 항목 모두 반영 완료.)

---

### #9 — 2026-09-02 04:38

배경: inbox #8의 Python(Pillow) 절차적 드로잉 파이프라인으로 플레이어 캐릭터/걷기 애니메이션을 만들었지만, 결과 품질이 기대에 못 미쳤다(순수 코드로 도형을 그리는 방식의 한계). 사용자가 PixelLab(픽셀아트 전용 AI 이미지 생성 서비스)의 API 키를 갖고 있어서, 사람이 직접 API를 테스트해 실제로 작동함을 확인했다. **이 지시는 inbox #8의 Pillow 파이프라인을 대체한다** — 앞으로 실제 캐릭터/동물 스프라이트는 PixelLab API로 만든다(단, 이미 만든 플레이어 스프라이트/애니메이션을 지금 당장 다시 만들라는 뜻은 아니다 — 그건 범위 밖, 아래 5번 참고).

## PixelLab API 사용법 (사람이 직접 확인한 사실)

- 인증: HTTP 헤더 `Authorization: Bearer $PIXELLAB_API_KEY` (키는 환경변수로 이미 주어짐 — CLAUDE.md의 "비밀값" 규칙 참고, 절대 하드코딩 금지)
- Base URL: `https://api.pixellab.ai/v2`
- **정적 이미지 후보 여러 장 생성**: `POST /generate-image-v2` — body: `{"description": "...", "image_size": {"width": W, "height": H}}`. width/height를 42 이하로 주면 **64장**, 43~85면 16장, 86~170이면 4장이 한 번에 나온다(장수가 많을수록 장당 단가가 훨씬 쌈 — 64장에 20 generation credits, 장당 약 0.31 크레딧 확인함). 응답은 `202`+`background_job_id`를 즉시 반환하는 비동기 방식이다.
- **작업 결과 폴링**: `GET /background-jobs/{job_id}` (같은 Bearer 인증). `status`가 `"completed"`가 될 때까지 몇 초 간격으로 반복 조회. 완료되면 `last_response.images`가 `{"type":"base64","base64":"..."}` 객체의 배열로 옴(위 예시 기준 64개).
- **애니메이션 생성**: `/animate-with-text`, `/animate-with-text-v2`, `/animate-with-text-v3`, `/animate-with-skeleton` 엔드포인트가 존재하는 것까지만 확인했고, 정확한 요청 파라미터는 확인 못했다 — **이번 세션이 `https://api.pixellab.ai/v2/openapi.json`을 직접 조회해서 정확한 스키마를 파악한 뒤 구현할 것.**
- 참고: `python3`와 `curl` 둘 다 하네스 허용 도구에 이미 추가돼 있다. Python `requests` 라이브러리도 설치되어 있다(`import requests` 가능).

## 지시 내용 (우선순위 순 — 규칙 4(기능 하나만)에 따라 여러 세션에 걸쳐 순서대로 처리할 것)

1. **PixelLab 생성 도구 만들기**: 기존 Pillow 도구(`tools/sprite_gen.py` 등, inbox #8에서 만든 것)를 대체/확장해서, `POST /generate-image-v2`로 후보를 요청하고 `GET /background-jobs/{id}`로 폴링해 결과를 받아오는 재사용 가능한 스크립트를 만든다. 받은 이미지들을 비교용 그리드/스트립 이미지로 합치는 기존 도구(inbox #8 3번에서 만든 것)를 그대로 재사용할 수 있으면 재사용한다.
2. **동물 스프라이트 첫 대상: 사슴부터**. `기존에 확립된 `assets/ART_STYLE.md`(캔버스 크기, 팔레트 등)를 먼저 읽고, 그 규칙과 최대한 맞는 프롬프트/크기로 사슴을 생성한다(다만 AI 생성물이라 절차적 도형과 정확히 같은 팔레트가 되긴 어려울 수 있음 — 최대한 근접하게 판단). 후보를 생성하고, Read 도구로 비교 이미지를 직접 보고 가장 나아 보이는 것을 고른다. 왜 그걸 골랐는지 `status.md`에 근거를 남긴다.
3. **사슴이 움직이도록 애니메이션 생성**: 2번에서 고른 이미지를 애니메이션 엔드포인트에 넣어 걷기(또는 이동) 동작 프레임을 만든다. 완성된 프레임들을 스트립 이미지로 합쳐서 Read 도구로 직접 보고 동작이 자연스러운지 판단 — 부자연스러우면 다시 시도한다.
4. **Godot에 실제로 반영**: 생성된 사슴 정적 이미지/애니메이션 프레임을 `assets/sprites/`에 저장하고, 기존 `scenes/Animal.tscn`/`scripts/animal.gd`에 실제 텍스처로 반영한다(지금은 절차적 사각형일 것). 관련 헤드리스 테스트로 회귀 확인.
5. **범위**: 이번 지시는 **동물**(사슴부터 시작해서 이후 다른 동물로 확장 가능)에 한정한다. **아이템(정적인 것)은 애니메이션이 필요 없다** — 후보 생성 후 1장 고르는 것까지만 하면 된다(움직이는 대상만 3번의 애니메이션 단계를 거친다). 플레이어 스프라이트를 PixelLab으로 다시 만드는 것, 무기/이펙트(총구 화염 등) 애니메이션은 이번 지시 범위 밖이다 — 나중에 별도 지시로 다룬다.

상태: 미처리
