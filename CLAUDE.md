# 하네스 프로토콜

이 저장소는 자동화된 세션(하네스)이 **사용자가 명시적으로 요청할 때마다 한 번씩** 실행되며 게임을 만든다(항상 백그라운드에서 도는 게 아니다 — 아래 규칙 7 참고). 매 세션은 아래 순서와 규칙을 그대로 따른다.

## 경로

- 프로젝트 루트: `/Users/tuna/projects/test_game`
- 기획서: `/Users/tuna/projects/test_game/design.md`
- 인수인계서: `/Users/tuna/projects/test_game/status.md` (최근 항목만 유지, 오래된 항목은 아래 아카이브로 이동)
- 인수인계서 아카이브: `/Users/tuna/projects/test_game/status_archive.md` (옮겨진 과거 항목 보관용, 참고용으로만 열어봄)
- 지시함: `/Users/tuna/projects/test_game/inbox.md`
- QA 테스트 기록: `/Users/tuna/projects/test_game/qa_test.md` (기능 하나 만들 때마다 하는 규칙 4의 QA와는 별개로, "전체 QA를 한 번 해봐" 같은 별도 QA 요청을 받았을 때 그 결과를 날짜/시간과 함께 기록하는 파일. `status.md`와 같은 append-only 규칙 — 새 번호로 추가만 하고 기존 항목은 안 건드림. QA 중 문제를 발견하면 `inbox.md`에 지시로 등록하고 그 inbox 번호를 여기 같이 남긴다.)
- Godot 실행파일: `godot` (PATH에 등록됨, `/opt/homebrew/bin/godot`)
- 원격 저장소: `https://github.com/kimtuna/test_game.git` (public, branch: `main`)

## 비밀값(API 키 등) — 반드시 지킬 것

이 저장소는 **public** GitHub 저장소다. API 키 같은 비밀값을 커밋되는 어떤 파일에도 **절대 직접 적지 않는다** (코드, 주석, 문서, status.md/inbox.md 포함 — 예시로라도 실제 키 값을 적지 않는다). 필요한 비밀값은 `.harness/secrets.env`(git에 안 올라가는 로컬 전용 파일)에 있고, 하네스 세션 실행 시 이미 환경변수로 주어져 있다 — 예: `PIXELLAB_API_KEY`. 코드에서는 환경변수로만 참조한다 (Python: `os.environ["PIXELLAB_API_KEY"]`, shell: `$PIXELLAB_API_KEY`). 새로운 외부 서비스 키가 필요한데 없다면, 코드로 만들어내려 하지 말고 `status.md`에 "이런 키가 필요하다"고 남긴 뒤 그 부분은 건너뛴다. (근거: 커밋 하나에라도 키가 들어가면 그 순간 전 세계에 공개된다 — git 히스토리에서 지우는 것도 매우 번거롭고, 이미 크롤링됐으면 지워도 소용없다.)

## 세션 시작 시 반드시 순서대로 읽기

1. `design.md` — 무엇을 만드는가. 초기 기획이며 거의 바뀌지 않는다. 여기서 게임의 목적과 범위를 벗어나지 않는다.
2. `status.md` — 어디까지 했고 다음은 무엇인가. 항목은 번호가 매겨져 있다. **가장 마지막 번호 항목이 이번 세션의 출발점**이며, 거기 적힌 "다음 할 일"을 그대로 이어받는다. **전체를 다 읽지 말고 마지막 5~10개 항목만 읽는다** (`tail`이나 `offset`을 써서 최근 부분만 확인). 더 오래된 판단 근거가 특별히 필요할 때만 `status_archive.md`를 추가로 찾아본다. (근거: 세션이 쌓일수록 이 파일이 계속 길어지는데, 매번 전체를 다 읽으면 이어받는 데 필요하지도 않은 옛 기록까지 토큰으로 소비하게 된다. 이어받는 데 필요한 건 최신 항목뿐이다.)
3. `inbox.md` — 사용자가 직접 내린 지시/아이디어. 번호가 매겨져 있고, `status.md`에 "처리 완료"로 기록되지 않은 가장 오래된 항목부터 반영한다. `status.md`의 다음 작업보다 `inbox.md`의 미처리 항목이 우선한다.

세 파일 모두 항목에 번호를 붙여 누적한다. 기존 항목은 절대 수정하거나 삭제하지 않고, 새 번호로 추가만 한다 (근거: 과거 기록을 지우면 다음 세션이 왜 그런 결정을 했는지 알 수 없게 되고, 사용자가 컴퓨터를 껐다 켜도 이어받을 지점을 잃는다).

`status.md`/`inbox.md`의 각 항목 제목에는 날짜뿐 아니라 **시간까지** 적는다. 형식: `### #N — YYYY-MM-DD HH:MM (한 줄 상황 요약)` (24시간제, 로컬 시간 기준. `date '+%Y-%m-%d %H:%M'`로 얻는다). (근거: 한 세션이 몇 분 만에 여러 개 쌓이는 경우가 많아 날짜만으로는 순서·간격을 구분할 수 없다. 사용자가 진행 속도나 특정 시점의 상태를 확인하려면 시간까지 있어야 한다.)

괄호 안에는 **이번 세션에서 실제로 무엇을 했는지**를 한 줄로 적는다 — 예: `(총 조준 방향 버그 수정)`, `(인벤토리 UI 구현)`, `(사용자 요청 처리: 저장 시스템)`. 세션이 어떻게 호출됐는지(사람이 방금 수동으로 시작했는지, 이미 돌고 있던 루프의 다음 반복인지)는 세션 스스로 확인할 방법이 전혀 없으니 **절대 단정해서 적지 않는다** — 오직 이번에 실제로 한 일을 근거로만 적는다. (근거: 확인할 수 없는 걸 사실처럼 적으면 다음 세션들이 그 문구를 그대로 베끼기만 하다가 실제 상황과 점점 멀어진다 — 실제로 "(수동 실행, 사용자가 하네스 사이클 1회를 직접 요청)"이라는 문구가 최초 한 번만 사실이었는데 그 뒤 여러 세션에 걸쳐 사실 확인 없이 그대로 복사되며 반복된 적이 있었다. 반면 "무엇을 했는지"는 세션이 직접 확인 가능한 사실이라 매번 정확하게 쓸 수 있고, 제목만 훑어도 이 프로젝트가 최근에 뭘 하고 있었는지 파악하기 쉬워진다.)

## 규칙

1. **합격 기준**: 이번에 만든 결과물을 다른 사람이 처음 봐도 품질이 떨어지지 않는가? (근거: 세션마다 만드는 사람이 바뀌는 것과 같으므로, 매 세션이 스스로 낮은 기준에 안주하면 전체 품질이 서서히 무너진다. "일단 동작하면 됨"이 아니라 "리뷰해도 부끄럽지 않음"이 기준이다.)
2. **세션 시작 순서**: 새 세션이 시작되면 `design.md` → `status.md` → `inbox.md` 순서로 반드시 읽는다. 항목이 추가될 때는 순서대로 번호를 붙이고, 직전 세션이 `status.md`에 남긴 지시를 이어서 진행한다. (근거: 세션은 매번 기억이 없는 상태로 시작하므로, 파일에 남긴 기록만이 유일한 연속성이다. 순서를 지켜야 최신 지시가 오래된 기획에 묻히지 않는다.)
3. **규칙에는 근거를 붙인다**: `status.md`나 `inbox.md`에 새 규칙/결정을 적을 때는 반드시 이유를 함께 적는다. (근거: 이유 없는 규칙은 다음 세션이 왜 지켜야 하는지 판단할 수 없어 임의로 무시하거나 잘못 해석하기 쉽다.)
4. **한 바퀴의 순서**: 읽기 → 하나 만들기 → 확인(QA) → 커밋 → 기록. 이 순서를 바꾸지 않는다.
   - 읽기: 위 세 파일을 순서대로 읽는다.
   - 하나 만들기: 이번 세션에서는 기능을 **하나만** 구현한다. 여러 개를 동시에 벌이지 않는다. (근거: 한 세션이 끝나면 기억이 사라지므로, 범위를 넓게 잡으면 미완성 상태로 세션이 끝나 다음 세션이 무엇이 완료됐는지 판단할 수 없다.)
   - 확인(QA): 최소한 `godot --headless --path /Users/tuna/projects/test_game --quit` 를 실행해 파싱/런타임 에러가 없는지 확인한다. 에러가 있으면 커밋하지 않고 고친 뒤 다시 확인한다. 규칙 1의 합격 기준(품질)도 이 단계에서 스스로 점검한다.
     - `tests/`의 헤드리스 테스트는 **이번에 건드린 기능과 직접 관련된 것 + 기본 quit 체크**만 돌린다. 테스트 전체(현재 20종 이상)를 매번 다 재실행하지 않는다 — 여러 시스템에 걸친 광범위한 리팩터링일 때만 예외적으로 전체를 돌린다. (근거: 테스트 수가 계속 늘어나는 구조라, 매 사이클 전부 재실행하면 사이클당 비용이 시간이 지날수록 계속 불어난다. 관련 없는 영역은 애초에 건드리지 않았으므로 회귀 위험도 낮다.)
     - **QA는 성격이 다른 세 갈래로 나눠서 생각한다(사용자가 명시적으로 이렇게 분류함, 2026-09-02).** 매번 셋 다 할 필요는 없고, 이번에 만든 기능과 관련된 갈래만 적용하면 된다 — 순수 데이터/로직 변경처럼 눈으로 볼 것도 상호작용할 것도 없는 작업은 아래 2·3번을 생략해도 된다.
       1. **자동검사 QA** — 사람이 매번 눈으로 보지 않아도 스크립트/도구로 기계적으로 판정하는 항목. 각 항목은 이름을 붙여 관리한다(아래는 현재 이 프로젝트에 맞는 목록이며, 새 시스템이 생기면 이름을 추가한다):
          - `text_overflow_check`: Label/RichTextLabel 등 UI 텍스트가 화면 또는 소속 컨테이너 밖으로 넘어가지 않는가.
          - `brightness_check`: 소품(나무·식물·장비 아이콘 등) 텍스처의 평균 밝기가 바닥 텍스처보다 밝지 않은가(밝으면 바닥에서 붕 떠 보이는 위화감이 생긴다는 미술 기준).
          - `reachable_area_check`: 충돌체·경계 배치 때문에 캐릭터가 도달할 수 없는 고립된 영역이 섬 안에 생기지 않는가.
          - `island_leash_check`: 동물이 섬(바다) 경계 밖으로 못 나가는가 — 현재 `tests/animal_boundary_flee_headless_test.gd`가 이 역할을 부분적으로 하고 있으니, 동물 이동 로직을 건드릴 때마다 이 테스트도 함께 돌린다.
          - `attack_anim_phase_check`: 판정이 있는 동작(마취총 발사, 향후 추가될 도구/무기 사용 등)이 예고(윈드업) → 판정 → 후딜 세 단계로 구분돼 있는가, 한 프레임짜리 즉발 판정으로 보이지 않는가.
          이 항목들은 사람이 매번 스크린샷을 보고 판단하는 것보다 한 번 도구를 만들어두고 이후 사이클마다 그 도구를 돌리는 쪽이 정확하고 저렴하다 — 아직 도구가 없는 항목은 이 세션이 즉석에서 만들려 하지 말고 `inbox.md`에 "이 이름의 자동검사 도구를 만들 것"이라는 지시로 등록해 별도 사이클(규칙 4의 "기능 하나만"에 따라 도구 제작 자체를 그 사이클의 유일한 작업으로)이 만들게 한다.
       2. **플레이 QA(테스터 QA)** — 실제 상호작용 시나리오를 표로 정리하고 그 값을 전부 실제로 굴려 결과가 기대와 맞는지 수치로 확인한다. 예:
          - `hit_check`: 사냥/포획 판정 — 사용 가능한 캐릭터 커스터마이징/장비 조합과 도구(마취총 등)를 바꿔가며 실제로 여러 번 쏴서 데미지·포획 성공 여부가 등급/장비 수치와 맞게 나오는지 확인한다.
          - `projectile_range_check`: 사거리 판정 — `tests/animal_ranged_hunt_headless_test.gd`가 이미 하듯, 동물을 사거리 경계값(안/밖) 여러 거리에 세워두고 조준 방향을 바꿔가며 명중/빗나감 표의 값을 전부 실제로 검증한다. 새 무기/도구가 생기면 그 무기의 사거리·판정표도 같은 방식으로 만든다.
          이번에 만들거나 건드린 기능이 전투·상호작용·수치 계열이면 관련 표를 최소 한 번은 전부 실행한다 — `tests/`에 이미 그 시나리오를 다루는 헤드리스 테스트가 있으면 그걸 실행하는 것으로 충분한 경우가 많다. (참고: 이 프로젝트는 현재 "동물의 숲 스타일" 평화로운 라이프시뮬레이션이라 플레이어가 적에게 공격받는 시스템 자체가 아직 없다 — 그런 시스템이 design.md에 추가되기 전까지 "플레이어가 공격을 받아보는" 시나리오는 적용 대상이 아니다.)
       3. **시각 QA** — 실제 창을 띄워 게임을 실행하고 스크린샷을 찍어 Read 도구로 이미지를 직접 눈으로 확인한다. 이번에 만든 게 눈에 보이는 동작/배치와 관련 있다면(움직임, 방향, 애니메이션, UI 배치, 충돌·경계 근처 동작 등) 매 사이클 기본 단계로 삼고, 최소한 아래 항목을 전부 체크한다:
          - 화풍·원근감이 기존 화면과 통일돼 있는가.
          - 외곽선이 일정한가(뭉개짐·떨림 없음).
          - 장비/도구를 든 경우 그 장비가 캐릭터 손 위치에 실제로 붙어 있는가(붕 떠 있거나 어긋나지 않는가).
          - 그 외 배치 깨짐, 겹침, 잘림 등 눈에 띄는 이상이 없는가.
          `--headless` 없이 `godot --path .`로 실제 창을 띄우고, 이번에 만든 기능과 관련된 조작을 실제로 몇 번 해본 뒤(예: 캐릭터를 움직여보거나 새로 만든 대상과 상호작용해보거나, 몇 초 기다려서 자동으로 일어나는 동작을 관찰), `screencapture`(또는 안 되면 엔진 내부 `get_viewport().get_texture()` 캡처)로 스크린샷을 찍는다. 확인이 끝나면 반드시 그 프로세스를 종료(`pkill`)해서 창이 계속 떠 있지 않게 한다. (근거: 사용자가 직접 플레이하다가 방향이 안 맞거나 경계에서 멈추거나 배회를 안 하는 문제들을 발견했는데, 전부 헤드리스 테스트만으로는 잡히지 않는 종류였다 — 헤드리스는 좌표/상태값 같은 논리적 정합성만 확인할 뿐 실제로 보고 조작했을 때 이상한지는 확인 못한다.)
     - **위 세 갈래 중 어느 것이든, 검사 중 이번에 만든 기능과 무관하더라도 뭔가 어색하거나 잘못됐다고 판단되면(배치가 깨짐, 스프라이트가 겹침, 색이 이상함, 예상과 다른 동작 등), 이번 세션 범위를 벗어나 직접 고치지 않더라도 반드시 `inbox.md`에 위 "지시함" 형식(`### #N — YYYY-MM-DD HH:MM` + 근거) 그대로 새 번호를 붙여 등록한다.** `status.md`의 "남은 제약"에만 적어두면 사람이 직접 `inbox.md`로 옮겨적기 전까지는 다음 사이클이 그 문제를 자동으로 집어가지 않는다 — 반면 `inbox.md`에 등록해두면 규칙 3(미처리 inbox 항목이 `status.md`의 다음 작업보다 우선한다)에 따라 다음 명시적 kickstart 때 자동으로 이어받아 고치는 루프가 만들어진다. (근거: 사용자가 "화면을 직접 검사해서 QA로 만들고, 어색하거나 잘못된 부분이 있으면 다시 inbox.md에 넣어서 다시 루프를 돌리게 만들어달라"고 명시적으로 요청함(2026-09-02) — 발견한 문제가 산문으로만 status.md에 묻히면 다음 세션이 그걸 실제 할 일로 인식하지 못하고 넘어가기 쉽다.)
   - 커밋: QA를 통과한 변경사항만 커밋한다. 커밋 메시지 첫 줄(제목)은 무엇을 왜 만들었는지 한 줄로 남긴다. 제목 아래 본문에는 아래 "기록"의 `요약:`과 같은 내용(무엇을 했는지 + 이번에 있었던 흥미로운 사실)을 2~4문장으로 적는다 — `status.md`를 열지 않고 `git log`나 GitHub 커밋 화면만 봐도 핵심을 알 수 있어야 한다. 커밋 후 `git push origin main`까지 수행한다.
   - 기록: `status.md`에 새 번호로 항목을 추가할 때, **제목을 쓰고 바로 다음 줄에 아래 형식을 그대로 써서 시작한다** (계기/한 일 등 다른 어떤 줄보다도 먼저):
     ```
     ### #N — YYYY-MM-DD HH:MM (한 줄 상황 요약)

     요약: (이번 세션에서 무엇을 했는지 한두 문장) + (QA 중 발견한 흥미로운 사실이 있었다면 — 예: 뭔가 부자연스럽게 동작해서 원인을 찾아 어떻게 고쳤는지, 예상 못한 버그, 중요한 판단 근거 등 — 이어서 한두 문장)

     - 계기: ...
     - 한 일: ...
     (이하 지금까지 해오던 대로 확인(QA)/남은 제약/다음 할 일을 전부 자세히 적는다)
     ```
     `요약:` 줄은 **선택이 아니라 매번 필수**다 — 이번 세션이 새 기능을 구현했든, 할 게 없어서 그냥 확인만 했든 상관없이 반드시 쓴다. 이 한두 문장만 읽어도 이번 세션에서 무슨 일이 있었는지 알 수 있어야 한다. 그 아래 상세 항목(계기/한 일/확인/남은 제약/다음 할 일)은 요약이 대신하는 게 아니라 그대로 자세히 유지한다. `inbox.md`의 항목을 처리했다면 "처리 완료 (status.md #N에서 반영)"라고 표시한다. (근거: 세션이 쌓일수록 기록이 길고 자세해져서, 사용자가 진행 상황을 확인할 때마다 매번 전체를 다 읽기 힘들어졌다. 맨 위 요약만 훑어도 핵심을 알 수 있고, 자세히 알고 싶을 때만 아래로 내려가서 읽으면 되게 만든다. 처음에 "요약: 줄부터 먼저 쓴다"라고만 적었더니, 세션이 사람에게 보내는 최종 응답 메시지에는 요약을 넣으면서 정작 `status.md` 안에는 빠뜨린 적이 있었다 — 그래서 실제 파일에 들어갈 형식을 코드 블록으로 명시했다.)
     - 이번 세션 중 에러/QA 실패가 발생했다가 해결된 경우, 그 부분은 GitHub Alert 문법 `> [!CAUTION]`로 감싸서 빨간색으로 강조한다 (GitHub가 `.md` 파일을 렌더링할 때 이 문법을 빨간 테두리+아이콘 박스로 표시한다. 일반 마크다운은 글자 색을 지정할 수 없어서 이 문법을 쓴다). 형식:
       ```
       > [!CAUTION]
       > 무엇이 문제였는지 (에러 메시지 요약) — 원인 — 어떻게 고쳤는지 — 재확인 결과
       ```
       (근거: 사용자가 GitHub에서 기록을 훑어볼 때, 문제 없이 진행된 세션과 에러가 있었던 세션을 스크롤만으로 바로 구분할 수 있어야 한다.)
5. **QA 통과 후에는 화면을 보기 전에 먼저 커밋한다**: `godot` 에디터를 열어 시각적으로 확인하는 것보다 커밋이 먼저다. (근거: 화면을 먼저 보면 "이왕 열어본 김에" 하며 범위를 벗어난 수정을 계속하게 되어 세션이 끝나지 않는다. QA를 통과한 시점의 상태를 먼저 확정(커밋)해 두면, 이후 무엇을 보든 최소한 그 지점으로 안전하게 되돌아갈 수 있다.)
6. **`status.md`가 길어지면 정리한다**: 항목이 30개를 넘으면, 오래된 항목(최근 10개를 제외한 나머지)을 `status_archive.md` 맨 뒤에 그대로 옮겨 붙이고 `status.md`에는 최근 10개만 남긴다. 옮기는 것이지 지우는 게 아니므로 "기존 항목 수정/삭제 금지" 규칙 위반이 아니다. (근거: 토큰 절감 규칙 2와 같은 이유 — 파일이 무한정 길어지면 매 세션 읽기 비용도 무한정 늘어난다.)
7. **이 하네스는 사용자가 명시적으로 요청할 때만 한 사이클을 돈다.** `.harness/run_cycle.sh`는 이제 무한 루프가 아니라 **정확히 한 사이클만 실행하고 끝난다**(launchd `RunAtLoad=false`, `KeepAlive` 없음 — kickstart로 명시적으로 깨워야만 실행된다). 그래서 이 세션은:
   - `inbox.md`에 처리할 게 아무리 많이 남아있어도, 이번 세션에서는 규칙 4대로 **하나만** 처리하고 세션을 마친다 — 다음 사이클을 스스로 이어서 시작할 방법도 없고, 그럴 필요도 없다(어차피 다음 사이클은 사람이 다시 명시적으로 요청해야 시작된다).
   - `design.md` 로드맵의 각 항목이 각각 **최소 하나의 실질적 구현 + 관련 QA 통과** 상태에 도달했고(완료 기준은 "다듬어져서 완벽한가"가 아니라 "만들어졌고 QA를 통과했는가"), `inbox.md`에도 미처리 항목이 없다면, **`status.md`가 스스로 제안했던 "다음 후보"를 세션이 알아서 실행하지 않는다.** 그런 아이디어는 "다음에 사용자가 참고하도록" `status.md`에 제안으로만 적어두고, 실제로 코드를 만들지는 않는다. (근거: 완료 기준을 만족한 뒤에는, 확장 여부를 세션이 스스로 판단해서 계속 진행하는 게 아니라 사용자가 실제로 플레이해보고 `inbox.md`로 지시할 때만 진행해야 한다 — 어차피 다음 사이클도 사용자 요청이 있어야만 시작되니, 이 판단은 그저 "이번 세션에서 뭘 만들지"의 기준일 뿐이다.)
   - 이 상태(더 처리할 지시가 없음)를 `status.md`에 남길 때는 `> [!IMPORTANT]` 다음 줄에 `> HARNESS_STOP: <한 줄 사유>`를 포함한다. 커밋 메시지 제목(첫 줄)에도 **`[자동 루프 종료]`**를 붙여 한눈에 보이게 한다 — 예: `docs: [자동 루프 종료] status.md #N — <무엇 때문에 멈췄는지>`. (근거: 지금은 launchd가 이 마커를 감지해서 반복을 멈추는 게 아니라 — 애초에 반복 자체가 없다 — `git log`나 GitHub 커밋 목록만 보고도 "이 시점에 더 할 지시가 없었다"를 바로 알 수 있게 하기 위한 기록 용도로 계속 쓴다.)

## 세션 종료

기록(`status.md` 커밋/푸시)까지 끝나면 세션을 종료한다. **다음 사이클은 자동으로 이어지지 않는다** — 사용자가 다시 명시적으로 요청해야(`launchctl kickstart -k gui/$(id -u)/com.kdw240.testgame.harness`) 다음 세션이 시작된다.
