# 하네스 프로토콜

이 저장소는 자동화된 세션(하네스)이 반복적으로 실행되며 게임을 만든다. 매 세션은 아래 순서와 규칙을 그대로 따른다.

## 경로

- 프로젝트 루트: `/Users/tuna/projects/test_game`
- 기획서: `/Users/tuna/projects/test_game/design.md`
- 인수인계서: `/Users/tuna/projects/test_game/status.md` (최근 항목만 유지, 오래된 항목은 아래 아카이브로 이동)
- 인수인계서 아카이브: `/Users/tuna/projects/test_game/status_archive.md` (옮겨진 과거 항목 보관용, 참고용으로만 열어봄)
- 지시함: `/Users/tuna/projects/test_game/inbox.md`
- Godot 실행파일: `godot` (PATH에 등록됨, `/opt/homebrew/bin/godot`)
- 원격 저장소: `https://github.com/kimtuna/test_game.git` (public, branch: `main`)

## 세션 시작 시 반드시 순서대로 읽기

1. `design.md` — 무엇을 만드는가. 초기 기획이며 거의 바뀌지 않는다. 여기서 게임의 목적과 범위를 벗어나지 않는다.
2. `status.md` — 어디까지 했고 다음은 무엇인가. 항목은 번호가 매겨져 있다. **가장 마지막 번호 항목이 이번 세션의 출발점**이며, 거기 적힌 "다음 할 일"을 그대로 이어받는다. **전체를 다 읽지 말고 마지막 5~10개 항목만 읽는다** (`tail`이나 `offset`을 써서 최근 부분만 확인). 더 오래된 판단 근거가 특별히 필요할 때만 `status_archive.md`를 추가로 찾아본다. (근거: 세션이 쌓일수록 이 파일이 계속 길어지는데, 매번 전체를 다 읽으면 이어받는 데 필요하지도 않은 옛 기록까지 토큰으로 소비하게 된다. 이어받는 데 필요한 건 최신 항목뿐이다.)
3. `inbox.md` — 사용자가 직접 내린 지시/아이디어. 번호가 매겨져 있고, `status.md`에 "처리 완료"로 기록되지 않은 가장 오래된 항목부터 반영한다. `status.md`의 다음 작업보다 `inbox.md`의 미처리 항목이 우선한다.

세 파일 모두 항목에 번호를 붙여 누적한다. 기존 항목은 절대 수정하거나 삭제하지 않고, 새 번호로 추가만 한다 (근거: 과거 기록을 지우면 다음 세션이 왜 그런 결정을 했는지 알 수 없게 되고, 사용자가 컴퓨터를 껐다 켜도 이어받을 지점을 잃는다).

`status.md`/`inbox.md`의 각 항목 제목에는 날짜뿐 아니라 **시간까지** 적는다. 형식: `### #N — YYYY-MM-DD HH:MM (자동 세션)` (24시간제, 로컬 시간 기준. `date '+%Y-%m-%d %H:%M'`로 얻는다). (근거: 한 세션이 몇 분 만에 여러 개 쌓이는 경우가 많아 날짜만으로는 순서·간격을 구분할 수 없다. 사용자가 진행 속도나 특정 시점의 상태를 확인하려면 시간까지 있어야 한다.)

## 규칙

1. **합격 기준**: 이번에 만든 결과물을 다른 사람이 처음 봐도 품질이 떨어지지 않는가? (근거: 세션마다 만드는 사람이 바뀌는 것과 같으므로, 매 세션이 스스로 낮은 기준에 안주하면 전체 품질이 서서히 무너진다. "일단 동작하면 됨"이 아니라 "리뷰해도 부끄럽지 않음"이 기준이다.)
2. **세션 시작 순서**: 새 세션이 시작되면 `design.md` → `status.md` → `inbox.md` 순서로 반드시 읽는다. 항목이 추가될 때는 순서대로 번호를 붙이고, 직전 세션이 `status.md`에 남긴 지시를 이어서 진행한다. (근거: 세션은 매번 기억이 없는 상태로 시작하므로, 파일에 남긴 기록만이 유일한 연속성이다. 순서를 지켜야 최신 지시가 오래된 기획에 묻히지 않는다.)
3. **규칙에는 근거를 붙인다**: `status.md`나 `inbox.md`에 새 규칙/결정을 적을 때는 반드시 이유를 함께 적는다. (근거: 이유 없는 규칙은 다음 세션이 왜 지켜야 하는지 판단할 수 없어 임의로 무시하거나 잘못 해석하기 쉽다.)
4. **한 바퀴의 순서**: 읽기 → 하나 만들기 → 확인(QA) → 커밋 → 기록. 이 순서를 바꾸지 않는다.
   - 읽기: 위 세 파일을 순서대로 읽는다.
   - 하나 만들기: 이번 세션에서는 기능을 **하나만** 구현한다. 여러 개를 동시에 벌이지 않는다. (근거: 한 세션이 끝나면 기억이 사라지므로, 범위를 넓게 잡으면 미완성 상태로 세션이 끝나 다음 세션이 무엇이 완료됐는지 판단할 수 없다.)
   - 확인(QA): 최소한 `godot --headless --path /Users/tuna/projects/test_game --quit` 를 실행해 파싱/런타임 에러가 없는지 확인한다. 에러가 있으면 커밋하지 않고 고친 뒤 다시 확인한다. 규칙 1의 합격 기준(품질)도 이 단계에서 스스로 점검한다.
     - `tests/`의 헤드리스 테스트는 **이번에 건드린 기능과 직접 관련된 것 + 기본 quit 체크**만 돌린다. 테스트 전체(현재 20종 이상)를 매번 다 재실행하지 않는다 — 여러 시스템에 걸친 광범위한 리팩터링일 때만 예외적으로 전체를 돌린다. (근거: 테스트 수가 계속 늘어나는 구조라, 매 사이클 전부 재실행하면 사이클당 비용이 시간이 지날수록 계속 불어난다. 관련 없는 영역은 애초에 건드리지 않았으므로 회귀 위험도 낮다.)
   - 커밋: QA를 통과한 변경사항만 커밋한다. 커밋 메시지는 무엇을 왜 만들었는지 한 줄로 남긴다. 커밋 후 `git push origin main`까지 수행한다.
   - 기록: `status.md`에 새 번호로 이번 세션 요약(무엇을 했는지, 다음에 할 일)을 추가하고 커밋/푸시한다. `inbox.md`의 항목을 처리했다면 "처리 완료 (status.md #N에서 반영)"라고 표시한다.
     - 이번 세션 중 에러/QA 실패가 발생했다가 해결된 경우, 그 부분은 GitHub Alert 문법 `> [!CAUTION]`로 감싸서 빨간색으로 강조한다 (GitHub가 `.md` 파일을 렌더링할 때 이 문법을 빨간 테두리+아이콘 박스로 표시한다. 일반 마크다운은 글자 색을 지정할 수 없어서 이 문법을 쓴다). 형식:
       ```
       > [!CAUTION]
       > 무엇이 문제였는지 (에러 메시지 요약) — 원인 — 어떻게 고쳤는지 — 재확인 결과
       ```
       (근거: 사용자가 GitHub에서 기록을 훑어볼 때, 문제 없이 진행된 세션과 에러가 있었던 세션을 스크롤만으로 바로 구분할 수 있어야 한다.)
5. **QA 통과 후에는 화면을 보기 전에 먼저 커밋한다**: `godot` 에디터를 열어 시각적으로 확인하는 것보다 커밋이 먼저다. (근거: 화면을 먼저 보면 "이왕 열어본 김에" 하며 범위를 벗어난 수정을 계속하게 되어 세션이 끝나지 않는다. QA를 통과한 시점의 상태를 먼저 확정(커밋)해 두면, 이후 무엇을 보든 최소한 그 지점으로 안전하게 되돌아갈 수 있다.)
6. **`status.md`가 길어지면 정리한다**: 항목이 30개를 넘으면, 오래된 항목(최근 10개를 제외한 나머지)을 `status_archive.md` 맨 뒤에 그대로 옮겨 붙이고 `status.md`에는 최근 10개만 남긴다. 옮기는 것이지 지우는 게 아니므로 "기존 항목 수정/삭제 금지" 규칙 위반이 아니다. (근거: 토큰 절감 규칙 2와 같은 이유 — 파일이 무한정 길어지면 매 세션 읽기 비용도 무한정 늘어난다.)
7. **로드맵을 다 채우면 자동 루프를 멈춘다**: `design.md`의 "진행 방식에 대한 참고"(또는 그 시점의 design.md가 명시한 로드맵)에 나열된 항목들이 각각 **최소 하나의 실질적 구현 + 관련 QA 통과** 상태에 도달했다고 판단되면, 새 기능을 계속 찾아서 만들지 않는다. 대신:
   - `status.md`에 새 항목을 추가해 각 로드맵 항목이 어떻게 충족됐는지(어느 status.md 번호에서 구현됐는지) 근거와 함께 정리하고,
   - 그 항목 안에 다음 형식의 줄을 반드시 포함한다: `> [!IMPORTANT]` 다음 줄에 `> HARNESS_STOP: <한 줄 사유>`,
   - 평소처럼 commit/push까지 마치고 세션을 종료한다.
   - 완료 기준은 "다듬어져서 완벽한가"가 아니라 "만들어졌고 QA를 통과했는가"다. (근거: "완벽하게 다듬어질 때까지"를 기준으로 삼으면 이런 종류의 프로젝트는 원칙적으로 끝나지 않고, 그 사이 토큰 비용만 계속 나간다. 사용자가 명시적으로 "다 만들어서 끝이 아니라 QA까지 끝나면 멈추는 것"이라고 지시했다 — 자잘한 다듬기는 사용자가 실제로 플레이해보고 `inbox.md`로 필요한 것만 지시하는 편이 낫다.)
   - `.harness/run_cycle.sh`가 매 사이클 종료 후 `status.md`에서 `HARNESS_STOP`을 감지하면 반복문을 멈추고 launchd 등록도 해제한다. 즉 이 마커를 남기는 것이 곧 "자동화를 끈다"는 뜻이다 — 가볍게 남기지 말고, 실제로 로드맵이 채워졌을 때만 남긴다.

## 세션 종료

기록(`status.md` 커밋/푸시)까지 끝나면 세션을 종료한다. 다음 실행은 이 문서를 처음부터 다시 읽는 새 세션이 이어받는다.

## 자동 루프가 멈춘 뒤 재개하는 방법 (참고용, 세션이 직접 할 일은 아님)

규칙 7에 따라 `HARNESS_STOP`이 기록되면 데몬이 스스로 멈춘다. 사용자가 `inbox.md`에 새 지시를 남기고 `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kdw240.testgame.harness.plist`로 데몬을 다시 켜야 루프가 재개된다 — 자동으로 다시 켜지지 않는다. (근거: 사용자 동의 없이 계속 비용이 나가는 것을 막기 위함이다.)
